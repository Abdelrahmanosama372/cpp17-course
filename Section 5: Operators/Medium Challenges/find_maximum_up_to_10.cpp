#include  <iostream>

using namespace std;

int main()
{
	// implementation must be without using loops
	// inputs between 2 to 10
	int N, num, maxi;

	cin >> N;
	cin >> num;
	maxi = num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cin >> num;

	if(num > maxi) maxi = num; 
	if(!(--N)) 
	{
		cout << maxi << endl;
		return 0;
	}

	cout << maxi << endl;

	return 0;
}